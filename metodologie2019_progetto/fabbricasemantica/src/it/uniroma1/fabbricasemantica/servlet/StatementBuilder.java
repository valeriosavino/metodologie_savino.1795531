package it.uniroma1.fabbricasemantica.servlet;

public class StatementBuilder 
{
	/*creare classe builder per le connessioni,
	
	dovrà essere del tipo:
	
	PreparedStatement p = new StatementBuilder()
			.setQuery(query) => sarebbe c.prepareStatement(query) 
			.setString(1, word)
			.setString(2, description)
			.setString(3, translation)
			.setString(4, email);
			.executeUpdate(); => sarebbe build
	*/
}
